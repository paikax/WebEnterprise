@model List<WebEnterprise.Models.Contribution>

@{
    ViewBag.Title = "Coordinator Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="page-container">

    <h1 class="m-3">Coordinator Index</h1>

    @if (Model.Count > 0)
    {
        <table class="date-table">
            <thead>
                <tr>
                    <th>Title</th>
                    <th>Content</th>
                    <th>Comment</th>
                    <th>Submission Date</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var contribution in Model)
                {
                    <tr>
                        <td>@contribution.Title</td>
                        <td><textarea readonly>@contribution.Content</textarea></td>
                        <td><textarea class="commentCoor" readonly>@contribution.CoordinatorComment</textarea></td>
                        <td>@contribution.SubmissionDate.ToShortDateString()</td>
                        <td class="d-flex align-items-center">
                            @if (contribution.Status == "Pending")
                            {
                                <button type="button" class="approve-btn" data-contribution-id="@contribution.Id" id="none-Btn">
                                    <ion-icon name="checkmark" id="table-button" style="--color: #47f100;"></ion-icon>
                                </button>
                                <button type="button" class="reject-btn" data-contribution-id="@contribution.Id" id="none-Btn">
                                    <ion-icon name="close" id="table-button" style="--color: #cb3131;"></ion-icon>
                                </button>
                            }
                            else
                            {
                                <span class="badge badge-info">@contribution.Status</span>
                            }

                            @if ((DateTime.Now - contribution.SubmissionDate).TotalDays >= 14)
                            {
                                <a href="@Url.Action("AddComment", "Contributions", new { contributionId = contribution.Id })">
                                    <ion-icon name="pricetag-outline" id="table-button" style="--color: #aaaaaa;"></ion-icon>
                                </a>
                            }
                            else
                            {
                                <a href="@Url.Action("AddComment", "Contributions", new { contributionId = contribution.Id })">
                                    <ion-icon name="pricetag-outline" id="table-button" style="--color: #162938;"></ion-icon>
                                </a>

                            }
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>No contributions found.</p>
    }
</div>

@section Scripts {
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        $(document).ready(function () {
            $(".approve-btn").click(function () {
                var contributionId = $(this).data("contribution-id");
                updateContributionStatus(contributionId, "Approve");
            });

            $(".reject-btn").click(function () {
                var contributionId = $(this).data("contribution-id");
                updateContributionStatus(contributionId, "Reject");
            });

            function updateContributionStatus(contributionId, status) {
                $.ajax({
                    url: "@Url.Action("UpdateStatus", "Contributions")",
                    method: "POST",
                    data: { id: contributionId, status: status },
                    success: function (response) {
                        // Reload the page or update the UI as needed
                        location.reload(); // Example: Reload the page to reflect changes
                    },
                    error: function () {
                        // Handle errors
                        alert("An error occurred while updating the contribution status.");
                    }
                });
            }
        });
    </script>
}



